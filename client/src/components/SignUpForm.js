import React, { useState } from "react";
import { Button, Error, Input, FormField, Label, Textarea } from "../styles";

function SignUpForm(){
   return "Sign up form, present!"
}

export default SignUpForm;